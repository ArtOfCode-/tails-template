<%# #
  # Template for easily generating a select field from native JS objects.
  # Usage: include('select_field', {readonly: true | false, disabled: true | false, values: [['content', 'value']], selected: 'value'})
  #%>

<% readonly = typeof readonly === 'undefined' ? false : readonly %>
<% disabled = typeof disabled === 'undefined' ? false : disabled %>

<select class="form-control" name="<%= name %>" id="<%= id %>" <%= readonly ? 'readonly' : '' %> <%= disabled ? 'disabled' : '' %>>
    <option></option>
    <% values.forEach(x => { %>
        <% if (selected === x[1]) { %>
            <option value="<%= x[1] %>" selected><%= x[0] %></option>
        <% } else { %>
            <option value="<%= x[1] %>"><%= x[0] %></option>
        <% } %>
    <% }); %>
</select>